# SEO Prompt Checklist — Custom Template for Exbabel New Article on `/blog`

You are working inside the existing Exbabel website and its already-completed `/blog` system.

The blog architecture, design system, article template, routing, navigation, structured-data system, sitemap system, metadata implementation, styling, components, and responsive layouts have already been created.

**Do NOT redesign or rebuild the blog.**

**Do NOT create a second blog architecture.**

**Do NOT introduce a new CMS, framework, routing system, metadata system, or article template unless something is genuinely missing or broken.**

Your job is to take the finished article below and publish it as a new SEO-optimized article using the existing Exbabel `/blog` implementation.

---

# ARTICLE INPUT

## Finished article

[PASTE THE COMPLETE FINISHED ARTICLE HERE]

---

## SEO research / keywords

Primary keyword:

[INSERT PRIMARY KEYWORD]

Secondary keywords / related queries:

[INSERT SECONDARY KEYWORDS]

Optional supporting topics/entities:

[INSERT SUPPORTING TOPICS, QUESTIONS, ENTITIES OR SEMANTIC KEYWORDS]

Preferred slug, if already decided:

[INSERT SLUG OR WRITE "CHOOSE THE BEST SEO SLUG"]

Optional target search intent:

[INFORMATIONAL / COMMERCIAL INVESTIGATION / COMPARISON / HOW-TO / OTHER]

Optional featured image:

[INSERT IMAGE PATH / DESCRIPTION / "USE EXISTING BLOG IMAGE WORKFLOW"]

---

# PRIMARY OBJECTIVE

Publish this article as a new Exbabel article under:

`/blog/[ARTICLE-SLUG]`

using the existing Exbabel blog template and architecture.

The finished page should be:

* indexable
* crawlable
* technically SEO-sound
* semantically structured
* internally linked
* mobile friendly
* accessible
* fast
* factually accurate
* easy for Google to understand
* useful to human readers
* consistent with all existing Exbabel `/blog` pages

Do not merely paste the article onto a page.

Perform the following SEO implementation and QA checklist.

---

# 1. PRESERVE THE ARTICLE AND SEARCH INTENT

The article has already been researched and written.

Do not substantially rewrite it unless necessary.

Preserve:

* its central argument
* target keyword
* important secondary keywords
* examples
* comparisons
* explanations
* FAQs
* product information
* technical information
* search intent

You may make small changes when needed for:

* grammar
* factual accuracy
* heading hierarchy
* readability
* formatting
* internal linking
* current product information
* removing obvious keyword stuffing
* correcting an outdated Exbabel claim

Do NOT transform the article into generic AI marketing copy.

Do NOT add filler simply to increase article length.

Do NOT repeatedly insert the exact-match keyword unnaturally.

Prioritize usefulness and topical completeness over keyword density.

There is no required keyword-density percentage.

---

# 2. UNDERSTAND THE SEARCH INTENT

Before implementation, determine what someone searching the primary keyword is actually trying to accomplish.

Identify whether the query is primarily:

* informational
* how-to
* product research
* comparison
* commercial investigation
* troubleshooting
* educational

Ensure that the article satisfies that intent early rather than forcing the reader through unnecessary marketing copy.

The introduction should make it obvious what the article addresses.

Do not hide the answer deep in the page merely to increase engagement.

---

# 3. URL / SLUG

Create a short, descriptive URL underneath `/blog`.

Preferred format:

`https://www.exbabel.com/blog/descriptive-topic`

Use the production hostname convention already used by Exbabel.

The slug should:

* use lowercase
* use hyphens between words
* describe the primary topic
* usually contain the main keyword or a close natural variation
* avoid unnecessary words
* avoid dates unless the content is inherently date-specific
* avoid random IDs
* avoid query parameters
* avoid excessive length

Example:

Primary keyword:

`church translation system`

Good:

`/blog/church-translation-system`

Avoid:

`/blog/the-complete-ultimate-guide-to-the-best-church-translation-system-in-2026`

If I provided a preferred slug, use it unless there is a serious technical or SEO reason not to.

Do not change an already-published article URL casually.

---

# 4. SEO TITLE

Create a unique `<title>` for this article.

The SEO title should:

* accurately describe the article
* prominently communicate the primary topic
* naturally incorporate the primary keyword or close variation
* be compelling without clickbait
* distinguish this page from other Exbabel pages
* include Exbabel branding where appropriate
* avoid duplicate branding if the existing system automatically appends `| Exbabel`

Do not mechanically truncate a strong title merely to satisfy an arbitrary character limit.

Prioritize clarity and meaning.

Return the final SEO title in your completion report.

---

# 5. META DESCRIPTION

Create a unique meta description for this page.

It should:

* accurately summarize the article
* communicate the value of clicking
* naturally mention the topic
* match the page content
* avoid keyword stuffing
* avoid generic descriptions used on other articles

Treat the meta description primarily as search-result copy rather than a ranking trick.

Return the final meta description in your completion report.

---

# 6. H1

The article must have one clear primary H1 representing the main page topic.

The visible H1 does NOT need to exactly match the SEO `<title>`.

The H1 should:

* clearly communicate the topic
* naturally relate to the primary keyword
* be written for humans
* accurately represent the article

Do not create multiple competing primary H1 headings.

---

# 7. HEADING STRUCTURE

Convert the article into a logical semantic heading hierarchy.

Use:

H1 = article title

H2 = major sections

H3 = subsections inside H2 sections

H4 only when genuinely necessary.

Do not choose heading levels based purely on font size.

Do not skip heading levels unnecessarily.

Do not turn random sentences into headings merely to insert keywords.

Where the article already has good headings, preserve them.

When headings can naturally communicate important secondary queries, use them.

For example:

Instead of:

`Equipment`

Prefer:

`What Church Translation Equipment Do You Need?`

when that accurately represents the content and matches search intent.

Do not force this pattern unnaturally.

---

# 8. FEATURED SNIPPET / DIRECT ANSWER OPPORTUNITIES

Identify sections that answer clear questions.

Where appropriate, structure answers so search engines and AI systems can easily understand them.

Useful formats include:

* concise definition paragraph
* ordered steps
* unordered lists
* comparison tables
* question followed immediately by a concise answer

For definition-style topics, consider placing a direct 1–3 sentence explanation immediately underneath the relevant H2.

Do not rewrite the entire article around featured snippets.

Human readability comes first.

---

# 9. SEMANTIC HTML

Use the semantic article structure already established in the Exbabel blog.

Where applicable use:

`<main>`

`<article>`

`<header>`

`<section>`

`<nav>`

`<figure>`

`<figcaption>`

`<table>`

`<ul>`

`<ol>`

Do not implement meaningful content as arbitrary styled `<div>` elements when semantic HTML is appropriate.

Lists should be actual HTML lists.

Comparisons should use actual semantic tables when appropriate.

Quotes should use appropriate quote markup when appropriate.

---

# 10. TABLE OF CONTENTS

If the existing Exbabel article template supports a Table of Contents and this article is sufficiently long, populate it using the article's major H2 sections.

Create clean anchor IDs.

Example:

`#church-translation-equipment`

`#how-ai-church-translation-works`

`#church-translation-cost`

Use stable, descriptive IDs.

Do not create unnecessarily long or keyword-stuffed anchors.

Do not add a Table of Contents to a short article where it makes the page worse.

---

# 11. PRIMARY KEYWORD PLACEMENT

Make sure the primary topic is naturally clear from important areas of the document.

Ideally the main keyword or a natural close variation appears where appropriate in:

* SEO title
* H1
* introductory content
* at least one relevant section heading
* body content
* slug
* meta description when natural
* internal anchor text where appropriate

Do NOT force the exact keyword into every location if doing so sounds unnatural.

Do NOT create keyword density targets.

Do NOT repeat the keyword unnecessarily.

---

# 12. SECONDARY KEYWORDS AND TOPICAL COVERAGE

Use the provided secondary keywords as indicators of related user questions and concepts.

Do NOT treat them as a checklist of phrases that must each appear verbatim.

Instead ensure the article comprehensively discusses the underlying topics.

Where natural, use:

* synonyms
* closely related terminology
* entities
* technologies
* use cases
* problems
* comparisons
* related questions

The objective is to clearly establish what the article is about, not to manipulate keyword frequency.

---

# 13. INTERNAL LINKS — IMPORTANT

Inspect the existing Exbabel routes and existing blog articles.

Add useful contextual internal links where appropriate.

Potential destinations may include existing pages for:

* Exbabel Translate
* Exbabel Live
* pricing
* languages
* features
* how it works
* church translation
* livestream translation
* supported voices
* benchmark/performance information
* trial/signup
* demo
* security/privacy
* related blog articles

Only link to URLs that actually exist.

Do not invent routes.

Use descriptive anchor text.

Good:

`live AI church translation`

`Exbabel pricing`

`supported translation languages`

`live video translation`

Avoid repeatedly using:

`click here`

`read more`

`learn more`

unless the surrounding interface makes the destination obvious.

Aim for useful internal links rather than an arbitrary required number.

For a substantial article, several contextual internal links are usually appropriate.

Do not insert an internal link every time a keyword appears.

---

# 14. RELATED BLOG ARTICLES

Use the existing Exbabel related-article functionality if applicable.

Choose related posts based on genuine topic relevance.

Do not populate related articles randomly.

If the existing system supports manual article relationships or tags, configure them appropriately.

Do not create fake articles.

---

# 15. BLOG INDEX

Ensure this new article appears properly on:

`/blog`

using the blog system already implemented.

Verify:

* title
* excerpt
* image
* URL
* category if applicable
* date
* card layout

The `/blog` page should contain a crawlable link to the article.

Do not redesign `/blog`.

---

# 16. EXTERNAL LINKS AND SOURCE QUALITY

If the article contains factual claims that benefit from independent sourcing, use authoritative sources when appropriate.

Prefer:

1. primary documentation
2. government sources
3. official standards organizations
4. peer-reviewed research
5. authoritative first-party product documentation

Avoid low-quality SEO blogs as evidence when a primary source exists.

For Exbabel product claims, use Exbabel's actual current first-party information.

Do not add external links merely because someone claims outbound links are a ranking factor.

Links should help readers verify or understand information.

---

# 17. VERIFY EXBABEL PRODUCT CLAIMS

Before publishing, verify time-sensitive Exbabel information against the current source of truth.

This is particularly important for:

* pricing
* included hours
* product tiers
* supported languages
* voice counts
* latency
* supported platforms
* integrations
* livestream capabilities
* trial duration
* security claims
* privacy claims
* retention/storage claims
* availability of specific AI providers or voice engines
* uptime/SLA claims
* certifications
* standards
* features

Do not publish outdated product information simply because it exists in the draft.

If an article claim conflicts with the current site/product configuration:

make the smallest necessary correction while preserving the article's SEO intent.

Report any meaningful changes to me afterward.

Never invent a certification, performance statistic, compliance claim, SLA, feature, integration, language count, or price.

---

# 18. IMAGES

Use the existing Exbabel blog-image system.

Every meaningful image should:

* be relevant to the surrounding content
* have a descriptive filename where practical
* include useful alt text
* use responsive image behavior
* include width/height or otherwise prevent layout shift
* be appropriately compressed
* use the site's existing image optimization system

Alt text should explain the image to someone who cannot see it.

Do NOT stuff keywords into alt text.

Bad:

`best church translation system church translation equipment AI translation church`

Good:

`Church attendee listening to an Exbabel translated sermon on a smartphone`

Decorative images should follow the existing accessibility pattern and should not receive fake SEO-focused alt text.

---

# 19. FEATURED / HERO IMAGE

Make sure the article has an appropriate featured image using the existing Exbabel blog system.

The featured image should represent the article topic.

It should also work well as a social sharing image if the current implementation uses it for Open Graph.

Avoid generic unrelated stock images.

Do not lazy-load the main above-the-fold LCP image if the existing implementation correctly prioritizes it.

---

# 20. CANONICAL URL

Generate a self-referencing canonical URL for the final production article.

Example:

`https://www.exbabel.com/blog/article-slug`

The canonical must:

* use the production domain
* match the preferred URL
* not point to localhost
* not point to staging
* not point to Google Docs
* not point to `/blog`
* not point to the homepage
* not point to a different article

Keep the canonical URL consistent with the URL placed in the sitemap.

---

# 21. INDEXABILITY

Confirm that the page can be indexed.

Verify:

* no accidental `noindex`
* not blocked by robots.txt
* page does not require authentication
* important article content is crawlable
* article URL works directly
* article returns a successful response
* canonical is correct

Do not alter the global robots configuration unless genuinely necessary.

---

# 22. XML SITEMAP

Use the existing sitemap system.

Ensure the article is automatically or manually included according to the current architecture.

Use the final canonical URL.

If the existing sitemap supports `lastmod`, use the genuine modification date.

Do NOT create a second sitemap system.

Do not put:

* localhost URLs
* preview URLs
* staging URLs
* query parameter duplicates

into the production sitemap.

---

# 23. BLOGPOSTING STRUCTURED DATA

Use the structured-data system already implemented for Exbabel blog posts.

The article should have appropriate `BlogPosting` / Article structured data where the existing template supports it.

Populate applicable values accurately, including:

* headline
* description
* image
* datePublished
* dateModified
* author
* publisher
* url
* mainEntityOfPage

Do not invent values merely to fill schema properties.

Visible page information and structured data should agree.

If no individual human author has been supplied, use the existing Exbabel organizational author convention rather than inventing a person.

---

# 24. BREADCRUMB STRUCTURED DATA

Use the existing breadcrumb implementation.

Expected hierarchy will generally resemble:

Home > Blog > Article

Ensure visible breadcrumb links and structured-data values correspond correctly.

Do not create duplicate/conflicting breadcrumb systems.

---

# 25. DATES

Use legitimate publication and modification dates.

Do not automatically change `dateModified` every time the website deploys.

Only update the modified date when meaningful article content has actually changed.

Show dates according to the existing Exbabel blog design.

---

# 26. AUTHOR / TRUST SIGNALS

Use the existing Exbabel author system.

If I have not supplied an individual author, do not invent one.

Use:

`Exbabel`

or whatever organizational author representation the blog already uses.

Preserve existing:

* author
* publication date
* update date
* company information
* privacy links
* product links
* About links

where the template provides them.

---

# 27. OPEN GRAPH

Populate the existing Open Graph metadata system correctly.

Verify:

* `og:title`
* `og:description`
* `og:url`
* `og:type`
* `og:image`
* relevant article metadata already supported by the system

The title, description and URL should correspond to this article.

Do not leave metadata copied from another article.

---

# 28. X / TWITTER SOCIAL METADATA

Use the existing social metadata implementation.

Verify the card points to:

* this article's title
* this article's description
* this article's URL
* the appropriate featured image

Do not create duplicate social metadata implementations.

---

# 29. MOBILE

Test the article using the existing responsive layout.

Check:

* headings
* article width
* images
* TOC
* tables
* CTA sections
* breadcrumbs
* long URLs
* lists
* navigation
* related posts

There should be no page-level horizontal overflow.

Tables may use a responsive container where necessary.

Content should remain comfortably readable on phones.

---

# 30. PERFORMANCE

Do not degrade the existing blog performance.

Avoid introducing:

* new heavy libraries
* unnecessarily large JavaScript
* unoptimized images
* layout shifts
* render-blocking resources
* duplicate analytics
* unnecessary third-party scripts

Reuse the existing blog components.

Pay particular attention to:

* LCP
* CLS
* INP

Do not solve a simple article-formatting problem with a new JavaScript package.

---

# 31. ACCESSIBILITY

Preserve or improve the existing article accessibility.

Verify:

* heading hierarchy
* alt text
* semantic lists
* semantic tables
* keyboard accessibility
* focus states
* readable contrast
* meaningful link text
* appropriate link/button usage

Use links for navigation.

Use buttons for actions.

Do not make important information available only through color or imagery.

---

# 32. CALLS TO ACTION

Preserve useful Exbabel CTAs where appropriate.

Possible intents include:

* Start a Free Trial
* View Pricing
* See Exbabel Live
* Schedule a Demo
* Contact Exbabel

Use only real existing routes.

Do not invent URLs.

Do not overload an informational article with sales CTAs.

The article should remain useful editorial content first.

Use CTAs at natural high-intent points.

---

# 33. CONTENT DUPLICATION / CANNIBALIZATION CHECK

Before publishing, quickly inspect the other Exbabel blog articles and major landing pages.

Determine whether another page already targets essentially the same primary intent.

Do not create two near-identical articles targeting the same query without a strategic reason.

If another article overlaps heavily:

* preserve this article if it serves a meaningfully different intent, or
* flag the cannibalization risk to me

Do not automatically delete, redirect, merge, or substantially modify another page without permission.

---

# 34. TITLE / DESCRIPTION DUPLICATION CHECK

Make sure this article does not reuse another article's:

* SEO title
* meta description
* canonical
* slug

Each article should have unique page metadata.

---

# 35. BROKEN LINK CHECK

Verify links added to this article.

Check for:

* 404s
* incorrect routes
* typoed slugs
* localhost URLs
* staging URLs
* outdated Exbabel routes

Do not leave placeholder links.

---

# 36. ARTICLE QUALITY CHECK

Before finishing, ask:

Does this page actually provide a useful answer to the searcher?

Make sure the article:

* answers its core topic
* provides substance rather than filler
* has logical sections
* is readable
* explains terminology
* gives practical information
* avoids excessive repetition
* clearly distinguishes Exbabel product information from general educational material
* does not contain unsupported claims

Do not artificially lengthen it merely because competitors have long articles.

---

# 37. AI / SEARCH ENGINE READABILITY

Structure the article so major facts and concepts are explicit rather than implied.

Use clear relationships such as:

* X is...
* X works by...
* X differs from Y because...
* Churches need X when...
* The main options are...
* The process involves...
* The advantages include...

Do this naturally.

Do not generate awkward robotic prose solely for AI search systems.

The objective is clear factual writing that humans, traditional search engines, and AI retrieval systems can understand.

---

# 38. DO NOT USE OUTDATED SEO TACTICS

Do NOT:

* add a `meta keywords` tag
* target arbitrary keyword-density percentages
* hide keywords
* repeat keywords unnaturally
* create invisible SEO text
* add dozens of tags purely for ranking
* duplicate the article across multiple URLs
* artificially update publication dates
* create fake authors
* create fake reviews
* create fake ratings
* create fake statistics
* create fake citations
* add unnecessary schema simply because schema exists
* mass-link every keyword occurrence
* generate filler paragraphs to reach a predetermined word count

---

# 39. PRE-PUBLISH SEO QA CHECKLIST

Before declaring the article finished, verify each applicable item:

### URL & indexing

[ ] Article uses the existing `/blog` architecture.

[ ] Final slug is clean and descriptive.

[ ] Article URL loads successfully.

[ ] Page is indexable.

[ ] No accidental `noindex`.

[ ] Page is not blocked by robots.txt.

[ ] Canonical points to the correct production article URL.

[ ] Canonical and sitemap URL agree.

[ ] Article appears in the existing XML sitemap.

### Metadata

[ ] Unique SEO title.

[ ] Unique meta description.

[ ] Correct Open Graph title.

[ ] Correct Open Graph description.

[ ] Correct Open Graph URL.

[ ] Correct Open Graph image.

[ ] Correct social/X metadata.

[ ] No metadata accidentally copied from another article.

### Content structure

[ ] One logical H1.

[ ] H2/H3 hierarchy is sensible.

[ ] Lists use semantic list markup.

[ ] Tables use semantic table markup.

[ ] Article content satisfies primary search intent.

[ ] Primary keyword appears naturally.

[ ] Secondary topics are incorporated naturally.

[ ] No keyword stuffing.

[ ] No obvious repetitive filler.

### Internal linking

[ ] Relevant internal links have been added.

[ ] Anchor text describes the destination.

[ ] All Exbabel links point to real routes.

[ ] Article is linked from `/blog`.

[ ] Related articles/resources are configured where appropriate.

### Images

[ ] Featured image configured.

[ ] Relevant images have descriptive alt text.

[ ] Images are responsive.

[ ] Images are appropriately optimized.

[ ] Image dimensions/layout behavior avoid obvious CLS.

### Structured data

[ ] Existing BlogPosting/Article schema populated correctly.

[ ] Structured data matches visible content.

[ ] Author is accurate.

[ ] Publisher is accurate.

[ ] Publication date is accurate.

[ ] Modification date is accurate.

[ ] URL is accurate.

[ ] Breadcrumb schema is accurate if used.

[ ] No fake or irrelevant structured data.

### Trust & accuracy

[ ] Current Exbabel pricing verified if mentioned.

[ ] Product capabilities verified.

[ ] Language counts verified if mentioned.

[ ] Voice counts verified if mentioned.

[ ] Performance/latency claims verified if mentioned.

[ ] Privacy/security claims verified if mentioned.

[ ] Integrations verified if mentioned.

[ ] Unsupported claims removed or flagged.

### Technical quality

[ ] No broken internal links.

[ ] No localhost links.

[ ] No staging links.

[ ] No placeholder URLs.

[ ] No unnecessary dependency added.

[ ] Existing blog styling remains intact.

[ ] Mobile layout works.

[ ] No horizontal overflow.

[ ] Accessibility semantics remain intact.

[ ] Existing website build succeeds.

[ ] Existing lint/type checks pass where applicable.

---

# 40. DO NOT REDESIGN THE BLOG

This instruction is important.

The Exbabel `/blog` design and architecture are already complete.

For each new article:

* reuse existing article components
* reuse existing metadata components
* reuse existing layout
* reuse existing image system
* reuse existing related-post system
* reuse existing structured data
* reuse existing sitemap implementation
* reuse existing typography
* reuse existing responsive behavior

Only modify shared infrastructure if you discover an actual bug or missing SEO requirement.

Do not "improve" the visual design unless explicitly instructed.

---

# FINAL RESPONSE REQUIRED

After publishing the article, give me a short SEO implementation report.

Return:

**Article**

* Article title:
* Primary keyword:
* Final URL:
* Slug:

**Search metadata**

* SEO title:
* Meta description:
* Canonical:

**Page structure**

* H1:
* Major H2s:
* Featured image:
* Table of Contents added: Yes/No

**SEO implementation**

* BlogPosting schema: Yes/No
* Breadcrumb schema: Yes/No
* Added to sitemap: Yes/No
* Indexable: Yes/No
* Open Graph configured: Yes/No
* Social metadata configured: Yes/No

**Internal linking**
List the internal links you added and their destinations.

**Content changes**
List any changes you made to the supplied article.

**Claims verification**
List any Exbabel facts that were corrected, updated or could not be verified.

**SEO concerns**
Report:

* possible keyword cannibalization
* missing image
* weak internal-link opportunity
* unsupported claim
* duplicated metadata
* technical indexing issue
* or any other significant concern

If there are no significant issues, state that clearly.

Do not simply respond:

"SEO optimized."

Actually perform the implementation and validation first.
